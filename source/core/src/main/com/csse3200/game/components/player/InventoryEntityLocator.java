package com.csse3200.game.components.player;

import com.csse3200.game.entities.Entity;
import com.csse3200.game.entities.EntityService;
import com.csse3200.game.services.ServiceLocator;

// Finds the first entity with an InventoryComponent
public final class InventoryEntityLocator {
//    private InventoryEntityLocator() {}
//
//    public static Entity findEntityWithInventory() {
//        EntityService es = ServiceLocator.getEntityService();
//        for (Entity e : es.getEntities()) { //Need to add the getters for component and inventory component class
//            if (e.getComponent(InventoryComponent.class) != null) return e;
//        }
//        return null;
//    }
}